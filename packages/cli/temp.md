# CLI package

Planned commands: `agentscope init`, `configure`, `install <harness>`, `status`, `capture`, `report`, and `doctor`. Configuration belongs in `~/.agentscope`, not in a project by default. `AGENTSCOPE_HOME` is the explicit override for tests and CI.
