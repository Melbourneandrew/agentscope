# CLI package

Planned commands: `agent-scope init`, `configure`, `install <harness>`, `status`, `capture`, `report`, and `doctor`. Configuration belongs in the OS config directory under `agent-scope`, not in a project by default.
