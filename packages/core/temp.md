# Core package

Owns the stable, vendor-neutral trace contract and SDK. The source currently copied here is the AgentScope framework from SF Platform; the migration must preserve fixture compatibility before changing public contracts.
