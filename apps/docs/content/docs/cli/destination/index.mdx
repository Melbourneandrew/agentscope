---
title: Destination connections
description: Configure and inspect reusable named trace destination connections.
command: agentscope destination
commandContract: sha256:538119798f5e0d6d2cabf68865893b78cfedd63598ffd3a6e3d3b9bfb7c97e08
---

Destination commands operate on named connections described by reviewed first-party descriptors. Core owns the outer configuration document, credential references, and routing invariants; provider packages never own terminal output or configuration transactions.

## Syntax

```bash
agentscope destination <configure|list|inspect|unconfigure|delete|rotate>
```

## Output and exits

Group help uses `human`. Leaf commands additionally support their declared machine modes and fixed diagnostic-to-exit mapping.

## Automation

Configuration accepts non-secret JSON and explicit CI environment references. Secrets are never accepted as settings or rendered in results. Local data deletion is separate from unconfiguration and always requires confirmation.

## Diagnostics

Leaf command pages list their exact fixed diagnostics. Group parsing failures use the root `cli.usage` contract.

## Examples

```bash
agentscope destination list
agentscope destination configure langfuse --name primary --settings '{"endpoint":"https://example.com"}'
```
