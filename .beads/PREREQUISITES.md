# External prerequisite preflight

Checked 2026-08-16 for Bead `agentscope-vah.2.7` and reconciled with the merged hermetic-only Langfuse decision on 2026-08-25. This record inventories external state; it does not turn an available service into a product support claim. Native and release claims require the later evidence Beads named below.

| Prerequisite | Current evidence | Owner and verification | Fallback or follow-up |
| --- | --- | --- | --- |
| Local Node and pnpm | Node `v26.5.0`; pnpm `9.15.0`; repository declares Node `>=22` and pnpm `9.15.0` | Developer; `node --version`, `pnpm --version`, frozen install | Phase 1 pins and tests the supported Node baseline; CI remains authoritative rather than relying on the developer's newer Node version. |
| Local Docker | Docker `29.6.2`; Compose `v5.3.1`; macOS arm64 | Developer; `docker --version`, `docker compose version` | Hermetic work is unavailable only when Docker is absent; unit and artifact work remain runnable. |
| MockServer image | Official `mockserver/mockserver:7.4.0` is available for linux/amd64 and linux/arm64. OCI index digest: `sha256:fed9b2089e021947f785d1f0bfda3723352bb2c1634ce7b0bcd42dfd1b0fd02f` | Test platform; `docker buildx imagetools inspect mockserver/mockserver:7.4.0` | Phase 4 records the selected digest in its artifact inventory and fails rather than falling back to a moving tag. See the [official image documentation](https://hub.docker.com/r/mockserver/mockserver/). |
| GitHub merge policy | Public repository; `main` protected; squash-only merging; stale reviews dismissed; administrators enforced | Repository owner; GitHub repository and branch-protection APIs | Phase 11 keeps required-check names synchronized with workflows. |
| Protected release tags | No repository ruleset currently protects `v*` tag creation, update, or deletion | Release-lane owner; exact production ruleset API inspection, disposable-canary mutation evidence, and the first real annotated release tag as production creation proof | `agentscope-rk8.6` creates separate layered rulesets: a creation rule with narrow release-authority bypass and an update/deletion rule with no routine release-actor bypass. Production `v*` refs are never used as negative probes. |
| Required checks | `hermetic-integration`, `Quality`, `Unit tests`, and `Documentation` are strict required checks | Repository owner; branch-protection API and a known merged PR | Phase 11 replaces the scaffold integration meaning and adds evidence/supply-chain checks before changing protection. |
| GitHub Pages | Pages uses GitHub Actions; `github-pages` environment allows `main`; recent deploy succeeded; repository homepage is `https://melbourneandrew.github.io/agentscope/docs` | Documentation workflow owner; Pages API, deploy run, deployed-link check | Phase 11 verifies links/base paths and keeps Pages isolated from npm and destination credentials. |
| Immutable GitHub Releases | Repository enforcement is currently disabled and therefore no Release is an immutable evidence authority | Release-lane owner; repository immutable-release settings API before tag acceptance, then release and asset attestation verification | `agentscope-rk8.6` enables and independently verifies enforcement before any release transaction; the lane fails closed while it is disabled. |
| Native credential APIs | Current host is macOS arm64 and `/usr/bin/security` exists. No Windows Credential Manager or Linux Secret Service claim is proven locally | Credential adapter owner; contract fakes in Phase 5, trusted native runners in `agentscope-vah.12.7` | Unsupported or unavailable stores fail safely with no plaintext fallback. |
| GitHub-hosted native runners | Workflows can be authored for Linux, macOS, and Windows, but this repository has not yet executed the Agentscope native backend matrix | CI owner; `agentscope-vah.12.7` workflow evidence | Support documentation stays provisional until pinned runner evidence passes. |
| Langfuse automated evidence | Automated compatibility evidence is hermetic contract-fixture and request-ledger evidence only; no destination credentials or public endpoint are admitted | Repository owner; merged CI and Langfuse Blueprint decisions plus exact hermetic verifier evidence | Remote provider compatibility and visibility are not claimed. Any future remote verification requires a new earlier standalone Blueprint and explicit user approval. |
| Public npm identity | `agentscope-cli` is selected but remains unclaimed. An unauthenticated registry query returned `E404` on 2026-08-25, and this machine is not npm-authenticated | Package owner; interactive npm authentication, exact bootstrap-tarball inspection, and post-publication registry metadata | After the release-control Blueprint merges, `agentscope-rk8.7` performs the one-time inert bootstrap publication under the `bootstrap` dist-tag. The executable remains `agentscope`; local packing and exact-artifact tests do not require registry access. |
| npm trusted publishing | Not configurable until the selected package has been established once | Package/repository owner; npm trusted-publisher settings, protected `npm-release` GitHub environment, and a rejected inert OIDC-probe stage through the exact production trust path | After bootstrap, inspect and record the exact repository, workflow, environment, and `npm stage publish` binding; require two-factor approval and disallow long-lived token publication. Before any product tag, stage a uniquely versioned inert probe from protected `main`, verify its downloaded bytes and recorder evidence, and reject it through owner 2FA. Configuration inspection alone is not an OIDC proof. |

## Trust and isolation conclusions

- Artifact-first implementation is unblocked: the CLI can be packed, checksum-pinned, clean-installed, and tested without npm authentication, Langfuse credentials, or native runner access.
- No protected credential is required by pull-request integration tests. Model protocols use pinned MockServer; destination protocols use hermetic ledgers.
- Langfuse automated evidence remains hermetic and credential-free. Native credential evidence is a later trusted supported-platform gate, not a prerequisite for implementing its contract.
- The selected npm package name is an external ownership prerequisite until the inert bootstrap establishes it. Internal workspace names may remain stale only until the identity implementation change merges; publication remains blocked until the authenticated ownership and trusted-publisher controls are proven.
